#include "Terminate_OS2Fatjet.h"

void Terminate_OS2Fatjet()
{
    //==============================================
    // Terminate_OS2Fatjet:
    // This function gets called after the event looping.
    // This is where one does stuff that needs to happen after the event looping for category OS2Fatjet.
    //==============================================

    // To place eventlist writing or something that needs to happen post loop.
    // TODO: implement event list writing
}
